# Pathfinder local environment template
#
# Copy this file to .env for local development. Never commit .env or place
# credentials, tokens, private keys, or production values in this template.
#
# Environment variables are added only when an implementation component
# documents the variable, owner, purpose, allowed values, and safe local default.
# Secrets must be supplied through the approved secret-management mechanism.
